# Ingestion Service Dependencies
# ===============================

# Core
pydantic>=2.0.0
pydantic-settings>=2.1.0

# FastAPI
fastapi>=0.104.0
uvicorn>=0.24.0
python-jose[cryptography]>=3.3.0

# Async utilities
aiofiles>=23.0.0

# S3/MinIO storage
aioboto3>=12.0.0

# Database connectors
asyncpg>=0.29.0
aiomysql>=0.2.0
sqlalchemy[asyncio]>=2.0.0

# Vector store (Qdrant) - using latest client with query_points() API
qdrant-client>=1.16.0

# Keyword search (OpenSearch)
opensearch-py>=2.4.0

# HTTP client
aiohttp>=3.9.0
httpx>=0.25.0

# Redis client
redis>=5.0.0

# Task queue
celery>=5.3.0
kombu>=5.3.0

# Retry logic
tenacity>=8.2.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0
markdownify>=0.11.0

# Document parsing
PyMuPDF>=1.23.0
unstructured>=0.11.0
python-docx>=1.1.0
markdown>=3.5.0
chardet>=5.2.0
pytesseract>=0.3.10  # Optional, for OCR
Pillow>=10.0.0  # Required for OCR (image processing)
PyYAML>=6.0.0  # YAML frontmatter parsing

# Chunking / Tokenization
tiktoken>=0.5.0
spacy>=3.7.0
# Note: Run `python -m spacy download en_core_web_sm` to install the model

# Metadata Enrichment
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
langdetect>=1.0.9
python-dateutil>=2.8.0

# MIME type detection
python-magic>=0.4.27

# JSONPath for API connector
jsonpath-ng>=1.6.0

# OpenTelemetry (US-2.12)
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-otlp>=1.22.0
opentelemetry-exporter-prometheus>=0.43b0
opentelemetry-instrumentation-fastapi>=0.43b0
opentelemetry-instrumentation-celery>=0.43b0

# Prometheus client (US-2.12)
prometheus-client>=0.19.0

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
aioresponses>=0.7.0  # Mock aiohttp requests

# Integration testing
testcontainers>=3.7.0
psycopg2-binary>=2.9.0  # For test fixtures
boto3>=1.34.0  # For test fixtures

# Type checking
mypy>=1.8.0
types-aiofiles>=23.0.0
types-beautifulsoup4>=4.12.0
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
pyasn1>=0.6.2 # not directly required, pinned by Snyk to avoid a vulnerability
