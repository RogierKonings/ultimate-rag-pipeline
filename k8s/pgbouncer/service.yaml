apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: rag-pipeline
  labels:
    app: pgbouncer
    component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: pgbouncer
  selector:
    app: pgbouncer
