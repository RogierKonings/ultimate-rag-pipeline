apiVersion: v1
kind: Service
metadata:
  name: embedding-service
  namespace: rag-pipeline
  labels:
    app: embedding-service
    component: ml
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: embedding-service
