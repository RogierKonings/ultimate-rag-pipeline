apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: rag-pipeline
  labels:
    app: ingestion-service
    component: ingestion
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      name: http
  selector:
    app: ingestion-service
