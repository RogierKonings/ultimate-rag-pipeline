apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: rag-pipeline
  labels:
    app: llm-gateway
    component: ml
spec:
  type: ClusterIP
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
      name: http
  selector:
    app: llm-gateway
