apiVersion: v1
kind: Service
metadata:
  name: orchestrator-service
  namespace: rag-pipeline
  labels:
    app: orchestrator-service
    component: orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
      name: http
  selector:
    app: orchestrator-service
