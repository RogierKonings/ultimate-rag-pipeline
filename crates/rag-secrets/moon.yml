$schema: 'https://moonrepo.dev/schemas/project.json'
language: 'rust'
type: 'library'

project:
  name: 'rag-secrets'
  description: 'Secrets management for RAG pipeline'

fileGroups:
  sources:
    - 'src/**/*'
    - 'Cargo.toml'
  tests:
    - 'tests/**/*'

tasks:
  build:
    command: 'cargo build -p rag-secrets'
    inputs:
      - '@group(sources)'
    deps:
      - 'rag-types:build'
      - 'rag-config:build'
      - 'rag-encryption:build'

  check:
    command: 'cargo check -p rag-secrets'
    inputs:
      - '@group(sources)'

  test:
    command: 'cargo test -p rag-secrets'
    inputs:
      - '@group(sources)'
      - '@group(tests)'
    deps:
      - '~:build'

  lint:
    command: 'cargo clippy -p rag-secrets --'
    inputs:
      - '@group(sources)'

  format:
    command: 'cargo fmt -p rag-secrets'
    local: true

  format-check:
    command: 'cargo fmt -p rag-secrets -- --check'
