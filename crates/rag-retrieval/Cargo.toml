[package]
name = "rag-retrieval"
description = "Retrieval algorithms for RAG Pipeline - RRF fusion, reranking, hybrid search"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# Internal crates
rag-types = { path = "../rag-types" }
rag-vectorstore = { path = "../rag-vectorstore" }
rag-search = { path = "../rag-search" }
rag-cache = { path = "../rag-cache" }
rag-auth = { path = "../rag-auth" }
rag-config = { path = "../rag-config" }
rag-telemetry = { path = "../rag-telemetry" }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Async runtime
tokio = { workspace = true }

# Logging
tracing = { workspace = true }

# UUID
uuid = { workspace = true }

# HTTP client for embedding service
reqwest = { version = "0.11", features = ["json", "rustls-tls"], default-features = false }

# API framework
axum = "0.7"
tower = "0.4"
tower-http = { version = "0.5", features = ["trace", "cors", "timeout"] }

# Date/time
chrono = { version = "0.4", features = ["serde"] }

# Async utilities
futures = "0.3"

# Parallel processing
rayon = "1.8"

# Unicode normalization
unicode-normalization = "0.1"

# Hashing for cache keys
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
tokio-test = { workspace = true }
criterion = { version = "0.5", features = ["html_reports"] }
rand = "0.8"

[[bench]]
name = "rrf_benchmark"
harness = false

[lints]
workspace = true
