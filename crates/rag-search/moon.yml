$schema: 'https://moonrepo.dev/schemas/project.json'
language: 'rust'
type: 'library'

project:
  name: 'rag-search'
  description: 'OpenSearch integration for RAG pipeline'

fileGroups:
  sources:
    - 'src/**/*'
    - 'Cargo.toml'
  tests:
    - 'tests/**/*'

tasks:
  build:
    command: 'cargo build -p rag-search'
    inputs:
      - '@group(sources)'
    deps:
      - 'rag-types:build'
      - 'rag-config:build'
      - 'rag-telemetry:build'

  check:
    command: 'cargo check -p rag-search'
    inputs:
      - '@group(sources)'

  test:
    command: 'cargo test -p rag-search'
    inputs:
      - '@group(sources)'
      - '@group(tests)'
    deps:
      - '~:build'

  lint:
    command: 'cargo clippy -p rag-search --'
    inputs:
      - '@group(sources)'

  format:
    command: 'cargo fmt -p rag-search'
    local: true

  format-check:
    command: 'cargo fmt -p rag-search -- --check'
