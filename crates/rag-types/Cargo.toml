[package]
name = "rag-types"
description = "Core types for the RAG Pipeline - documents, chunks, embeddings, and common newtypes"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
axum = ["dep:axum"]
sqlx = ["dep:sqlx"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
validator = { workspace = true }
http = "1"
axum = { version = "0.7", optional = true }
sqlx = { version = "0.8", features = ["postgres", "macros"], optional = true, default-features = false }

[dev-dependencies]
tokio-test = { workspace = true }

[lints]
workspace = true
