# Auto-generated by scripts/generate_slo_rules.py
# DO NOT EDIT MANUALLY - changes will be overwritten

groups:
- name: slo_alerting_rules
  rules:
  - alert: SLOQuery_AvailabilityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_query_total{status=\"success\"}[5m])))\
      \ / (sum(rate(rag_query_total[5m]))))) > 0.014400000000000013\n            and\n\
      \            (1 - ((sum(rate(rag_query_total{status=\"success\"}[1h]))) / (sum(rate(rag_query_total[1h])))))\
      \ > 0.014400000000000013\n        )"
    for: 2m
    labels:
      severity: critical
      slo: query_availability
      burn_rate: '14.4'
    annotations:
      summary: SLO query_availability burn rate is 14.4x
      description: 'Error budget for query_availability is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/query_availability
  - alert: SLOQuery_AvailabilityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_query_total{status=\"success\"}[30m])))\
      \ / (sum(rate(rag_query_total[30m]))))) > 0.006000000000000005\n           \
      \ and\n            (1 - ((sum(rate(rag_query_total{status=\"success\"}[6h])))\
      \ / (sum(rate(rag_query_total[6h]))))) > 0.006000000000000005\n        )"
    for: 2m
    labels:
      severity: critical
      slo: query_availability
      burn_rate: '6.0'
    annotations:
      summary: SLO query_availability burn rate is 6.0x
      description: 'Error budget for query_availability is being consumed 6.0x faster
        than sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/query_availability
  - alert: SLOQuery_AvailabilityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_query_total{status=\"success\"}[6h])))\
      \ / (sum(rate(rag_query_total[6h]))))) > 0.0010000000000000009\n           \
      \ and\n            (1 - ((sum(rate(rag_query_total{status=\"success\"}[3d])))\
      \ / (sum(rate(rag_query_total[3d]))))) > 0.0010000000000000009\n        )"
    for: 2m
    labels:
      severity: warning
      slo: query_availability
      burn_rate: '1.0'
    annotations:
      summary: SLO query_availability burn rate is 1.0x
      description: 'Error budget for query_availability is being consumed 1.0x faster
        than sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/query_availability
  - alert: SLOQuery_AvailabilityErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - (sum(rate(rag_query_total{status=\"\
      success\"}[30d]))) / (sum(rate(rag_query_total[30d]))))\n            / 0.0010000000000000009\n\
      \        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: query_availability
    annotations:
      summary: SLO query_availability error budget exhausted
      description: 'The error budget for query_availability has been completely consumed.
        Target: 99.9% over 30d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/query_availability
  - alert: SLOLlm_AvailabilityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_llm_requests_total{status=\"success\"\
      }[5m]))) / (sum(rate(rag_llm_requests_total[5m]))))) > 0.07200000000000006\n\
      \            and\n            (1 - ((sum(rate(rag_llm_requests_total{status=\"\
      success\"}[1h]))) / (sum(rate(rag_llm_requests_total[1h]))))) > 0.07200000000000006\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: llm_availability
      burn_rate: '14.4'
    annotations:
      summary: SLO llm_availability burn rate is 14.4x
      description: 'Error budget for llm_availability is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/llm_availability
  - alert: SLOLlm_AvailabilityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_llm_requests_total{status=\"success\"\
      }[30m]))) / (sum(rate(rag_llm_requests_total[30m]))))) > 0.030000000000000027\n\
      \            and\n            (1 - ((sum(rate(rag_llm_requests_total{status=\"\
      success\"}[6h]))) / (sum(rate(rag_llm_requests_total[6h]))))) > 0.030000000000000027\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: llm_availability
      burn_rate: '6.0'
    annotations:
      summary: SLO llm_availability burn rate is 6.0x
      description: 'Error budget for llm_availability is being consumed 6.0x faster
        than sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/llm_availability
  - alert: SLOLlm_AvailabilityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_llm_requests_total{status=\"success\"\
      }[6h]))) / (sum(rate(rag_llm_requests_total[6h]))))) > 0.0050000000000000044\n\
      \            and\n            (1 - ((sum(rate(rag_llm_requests_total{status=\"\
      success\"}[3d]))) / (sum(rate(rag_llm_requests_total[3d]))))) > 0.0050000000000000044\n\
      \        )"
    for: 2m
    labels:
      severity: warning
      slo: llm_availability
      burn_rate: '1.0'
    annotations:
      summary: SLO llm_availability burn rate is 1.0x
      description: 'Error budget for llm_availability is being consumed 1.0x faster
        than sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/llm_availability
  - alert: SLOLlm_AvailabilityErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - (sum(rate(rag_llm_requests_total{status=\"\
      success\"}[30d]))) / (sum(rate(rag_llm_requests_total[30d]))))\n           \
      \ / 0.0050000000000000044\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: llm_availability
    annotations:
      summary: SLO llm_availability error budget exhausted
      description: 'The error budget for llm_availability has been completely consumed.
        Target: 99.5% over 30d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/llm_availability
  - alert: SLOQuery_LatencyBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.99, sum(rate(rag_query_duration_seconds_bucket[5m]))\
      \ by (le)))) > 0.14400000000000013\n            and\n            (1 - (histogram_quantile(0.99,\
      \ sum(rate(rag_query_duration_seconds_bucket[1h])) by (le)))) > 0.14400000000000013\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: query_latency
      burn_rate: '14.4'
    annotations:
      summary: SLO query_latency burn rate is 14.4x
      description: 'Error budget for query_latency is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/query_latency
  - alert: SLOQuery_LatencyBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.99, sum(rate(rag_query_duration_seconds_bucket[30m]))\
      \ by (le)))) > 0.06000000000000005\n            and\n            (1 - (histogram_quantile(0.99,\
      \ sum(rate(rag_query_duration_seconds_bucket[6h])) by (le)))) > 0.06000000000000005\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: query_latency
      burn_rate: '6.0'
    annotations:
      summary: SLO query_latency burn rate is 6.0x
      description: 'Error budget for query_latency is being consumed 6.0x faster than
        sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/query_latency
  - alert: SLOQuery_LatencyBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.99, sum(rate(rag_query_duration_seconds_bucket[6h]))\
      \ by (le)))) > 0.010000000000000009\n            and\n            (1 - (histogram_quantile(0.99,\
      \ sum(rate(rag_query_duration_seconds_bucket[3d])) by (le)))) > 0.010000000000000009\n\
      \        )"
    for: 2m
    labels:
      severity: warning
      slo: query_latency
      burn_rate: '1.0'
    annotations:
      summary: SLO query_latency burn rate is 1.0x
      description: 'Error budget for query_latency is being consumed 1.0x faster than
        sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/query_latency
  - alert: SLOQuery_LatencyErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - histogram_quantile(0.99, sum(rate(rag_query_duration_seconds_bucket[30d]))\
      \ by (le)))\n            / 0.010000000000000009\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: query_latency
    annotations:
      summary: SLO query_latency error budget exhausted
      description: 'The error budget for query_latency has been completely consumed.
        Target: 99.0% over 30d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/query_latency
  - alert: SLORetrieval_LatencyBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.99, sum(rate(rag_retrieval_duration_seconds_bucket[5m]))\
      \ by (le)))) > 0.14400000000000013\n            and\n            (1 - (histogram_quantile(0.99,\
      \ sum(rate(rag_retrieval_duration_seconds_bucket[1h])) by (le)))) > 0.14400000000000013\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: retrieval_latency
      burn_rate: '14.4'
    annotations:
      summary: SLO retrieval_latency burn rate is 14.4x
      description: 'Error budget for retrieval_latency is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency
  - alert: SLORetrieval_LatencyBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.99, sum(rate(rag_retrieval_duration_seconds_bucket[30m]))\
      \ by (le)))) > 0.06000000000000005\n            and\n            (1 - (histogram_quantile(0.99,\
      \ sum(rate(rag_retrieval_duration_seconds_bucket[6h])) by (le)))) > 0.06000000000000005\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: retrieval_latency
      burn_rate: '6.0'
    annotations:
      summary: SLO retrieval_latency burn rate is 6.0x
      description: 'Error budget for retrieval_latency is being consumed 6.0x faster
        than sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency
  - alert: SLORetrieval_LatencyBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.99, sum(rate(rag_retrieval_duration_seconds_bucket[6h]))\
      \ by (le)))) > 0.010000000000000009\n            and\n            (1 - (histogram_quantile(0.99,\
      \ sum(rate(rag_retrieval_duration_seconds_bucket[3d])) by (le)))) > 0.010000000000000009\n\
      \        )"
    for: 2m
    labels:
      severity: warning
      slo: retrieval_latency
      burn_rate: '1.0'
    annotations:
      summary: SLO retrieval_latency burn rate is 1.0x
      description: 'Error budget for retrieval_latency is being consumed 1.0x faster
        than sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency
  - alert: SLORetrieval_LatencyErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - histogram_quantile(0.99, sum(rate(rag_retrieval_duration_seconds_bucket[30d]))\
      \ by (le)))\n            / 0.010000000000000009\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: retrieval_latency
    annotations:
      summary: SLO retrieval_latency error budget exhausted
      description: 'The error budget for retrieval_latency has been completely consumed.
        Target: 99.0% over 30d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency
  - alert: SLOLlm_TtftBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.95, sum(rate(rag_llm_ttft_seconds_bucket[5m]))\
      \ by (le)))) > 0.7200000000000006\n            and\n            (1 - (histogram_quantile(0.95,\
      \ sum(rate(rag_llm_ttft_seconds_bucket[1h])) by (le)))) > 0.7200000000000006\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: llm_ttft
      burn_rate: '14.4'
    annotations:
      summary: SLO llm_ttft burn rate is 14.4x
      description: 'Error budget for llm_ttft is being consumed 14.4x faster than
        sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/llm_ttft
  - alert: SLOLlm_TtftBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.95, sum(rate(rag_llm_ttft_seconds_bucket[30m]))\
      \ by (le)))) > 0.30000000000000027\n            and\n            (1 - (histogram_quantile(0.95,\
      \ sum(rate(rag_llm_ttft_seconds_bucket[6h])) by (le)))) > 0.30000000000000027\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: llm_ttft
      burn_rate: '6.0'
    annotations:
      summary: SLO llm_ttft burn rate is 6.0x
      description: 'Error budget for llm_ttft is being consumed 6.0x faster than sustainable.
        Short window (30m) and long window (6h) both exceed threshold. Action: Page'
      runbook_url: https://runbooks.example.com/slo/llm_ttft
  - alert: SLOLlm_TtftBurnRateTooHigh
    expr: "(\n            (1 - (histogram_quantile(0.95, sum(rate(rag_llm_ttft_seconds_bucket[6h]))\
      \ by (le)))) > 0.050000000000000044\n            and\n            (1 - (histogram_quantile(0.95,\
      \ sum(rate(rag_llm_ttft_seconds_bucket[3d])) by (le)))) > 0.050000000000000044\n\
      \        )"
    for: 2m
    labels:
      severity: warning
      slo: llm_ttft
      burn_rate: '1.0'
    annotations:
      summary: SLO llm_ttft burn rate is 1.0x
      description: 'Error budget for llm_ttft is being consumed 1.0x faster than sustainable.
        Short window (6h) and long window (3d) both exceed threshold. Action: Create
        ticket'
      runbook_url: https://runbooks.example.com/slo/llm_ttft
  - alert: SLOLlm_TtftErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - histogram_quantile(0.95, sum(rate(rag_llm_ttft_seconds_bucket[7d]))\
      \ by (le)))\n            / 0.050000000000000044\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: llm_ttft
    annotations:
      summary: SLO llm_ttft error budget exhausted
      description: 'The error budget for llm_ttft has been completely consumed. Target:
        95.0% over 7d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/llm_ttft
  - alert: SLORetrieval_QualityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_query_total[5m])) - sum(rate(rag_retrieval_zero_results_total[5m])))\
      \ / (sum(rate(rag_query_total[5m]))))) > 2.8799999999999994\n            and\n\
      \            (1 - ((sum(rate(rag_query_total[1h])) - sum(rate(rag_retrieval_zero_results_total[1h])))\
      \ / (sum(rate(rag_query_total[1h]))))) > 2.8799999999999994\n        )"
    for: 2m
    labels:
      severity: critical
      slo: retrieval_quality
      burn_rate: '14.4'
    annotations:
      summary: SLO retrieval_quality burn rate is 14.4x
      description: 'Error budget for retrieval_quality is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/retrieval_quality
  - alert: SLORetrieval_QualityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_query_total[30m])) - sum(rate(rag_retrieval_zero_results_total[30m])))\
      \ / (sum(rate(rag_query_total[30m]))))) > 1.1999999999999997\n            and\n\
      \            (1 - ((sum(rate(rag_query_total[6h])) - sum(rate(rag_retrieval_zero_results_total[6h])))\
      \ / (sum(rate(rag_query_total[6h]))))) > 1.1999999999999997\n        )"
    for: 2m
    labels:
      severity: critical
      slo: retrieval_quality
      burn_rate: '6.0'
    annotations:
      summary: SLO retrieval_quality burn rate is 6.0x
      description: 'Error budget for retrieval_quality is being consumed 6.0x faster
        than sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/retrieval_quality
  - alert: SLORetrieval_QualityBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_query_total[6h])) - sum(rate(rag_retrieval_zero_results_total[6h])))\
      \ / (sum(rate(rag_query_total[6h]))))) > 0.19999999999999996\n            and\n\
      \            (1 - ((sum(rate(rag_query_total[3d])) - sum(rate(rag_retrieval_zero_results_total[3d])))\
      \ / (sum(rate(rag_query_total[3d]))))) > 0.19999999999999996\n        )"
    for: 2m
    labels:
      severity: warning
      slo: retrieval_quality
      burn_rate: '1.0'
    annotations:
      summary: SLO retrieval_quality burn rate is 1.0x
      description: 'Error budget for retrieval_quality is being consumed 1.0x faster
        than sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/retrieval_quality
  - alert: SLORetrieval_QualityErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - (sum(rate(rag_query_total[7d])) - sum(rate(rag_retrieval_zero_results_total[7d])))\
      \ / (sum(rate(rag_query_total[7d]))))\n            / 0.19999999999999996\n \
      \       )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: retrieval_quality
    annotations:
      summary: SLO retrieval_quality error budget exhausted
      description: 'The error budget for retrieval_quality has been completely consumed.
        Target: 80.0% over 7d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/retrieval_quality
  - alert: SLOCache_EffectivenessBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_cache_hits_total[5m]))) / (sum(rate(rag_cache_hits_total[5m]))\
      \ + sum(rate(rag_cache_misses_total[5m]))))) > 7.2\n            and\n      \
      \      (1 - ((sum(rate(rag_cache_hits_total[1h]))) / (sum(rate(rag_cache_hits_total[1h]))\
      \ + sum(rate(rag_cache_misses_total[1h]))))) > 7.2\n        )"
    for: 2m
    labels:
      severity: critical
      slo: cache_effectiveness
      burn_rate: '14.4'
    annotations:
      summary: SLO cache_effectiveness burn rate is 14.4x
      description: 'Error budget for cache_effectiveness is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/cache_effectiveness
  - alert: SLOCache_EffectivenessBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_cache_hits_total[30m]))) / (sum(rate(rag_cache_hits_total[30m]))\
      \ + sum(rate(rag_cache_misses_total[30m]))))) > 3.0\n            and\n     \
      \       (1 - ((sum(rate(rag_cache_hits_total[6h]))) / (sum(rate(rag_cache_hits_total[6h]))\
      \ + sum(rate(rag_cache_misses_total[6h]))))) > 3.0\n        )"
    for: 2m
    labels:
      severity: critical
      slo: cache_effectiveness
      burn_rate: '6.0'
    annotations:
      summary: SLO cache_effectiveness burn rate is 6.0x
      description: 'Error budget for cache_effectiveness is being consumed 6.0x faster
        than sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/cache_effectiveness
  - alert: SLOCache_EffectivenessBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_cache_hits_total[6h]))) / (sum(rate(rag_cache_hits_total[6h]))\
      \ + sum(rate(rag_cache_misses_total[6h]))))) > 0.5\n            and\n      \
      \      (1 - ((sum(rate(rag_cache_hits_total[3d]))) / (sum(rate(rag_cache_hits_total[3d]))\
      \ + sum(rate(rag_cache_misses_total[3d]))))) > 0.5\n        )"
    for: 2m
    labels:
      severity: warning
      slo: cache_effectiveness
      burn_rate: '1.0'
    annotations:
      summary: SLO cache_effectiveness burn rate is 1.0x
      description: 'Error budget for cache_effectiveness is being consumed 1.0x faster
        than sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/cache_effectiveness
  - alert: SLOCache_EffectivenessErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - (sum(rate(rag_cache_hits_total[7d])))\
      \ / (sum(rate(rag_cache_hits_total[7d])) + sum(rate(rag_cache_misses_total[7d]))))\n\
      \            / 0.5\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: cache_effectiveness
    annotations:
      summary: SLO cache_effectiveness error budget exhausted
      description: 'The error budget for cache_effectiveness has been completely consumed.
        Target: 50.0% over 7d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/cache_effectiveness
  - alert: SLORetrieval_Latency_P95BurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(retrieval_service_search_duration_seconds_bucket{le=\"\
      0.25\"}[5m]))) / (sum(rate(retrieval_service_search_duration_seconds_count[5m])))))\
      \ > 0.7200000000000006\n            and\n            (1 - ((sum(rate(retrieval_service_search_duration_seconds_bucket{le=\"\
      0.25\"}[1h]))) / (sum(rate(retrieval_service_search_duration_seconds_count[1h])))))\
      \ > 0.7200000000000006\n        )"
    for: 2m
    labels:
      severity: critical
      slo: retrieval_latency_p95
      burn_rate: '14.4'
    annotations:
      summary: SLO retrieval_latency_p95 burn rate is 14.4x
      description: 'Error budget for retrieval_latency_p95 is being consumed 14.4x
        faster than sustainable. Short window (5m) and long window (1h) both exceed
        threshold. Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency_p95
  - alert: SLORetrieval_Latency_P95BurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(retrieval_service_search_duration_seconds_bucket{le=\"\
      0.25\"}[30m]))) / (sum(rate(retrieval_service_search_duration_seconds_count[30m])))))\
      \ > 0.30000000000000027\n            and\n            (1 - ((sum(rate(retrieval_service_search_duration_seconds_bucket{le=\"\
      0.25\"}[6h]))) / (sum(rate(retrieval_service_search_duration_seconds_count[6h])))))\
      \ > 0.30000000000000027\n        )"
    for: 2m
    labels:
      severity: critical
      slo: retrieval_latency_p95
      burn_rate: '6.0'
    annotations:
      summary: SLO retrieval_latency_p95 burn rate is 6.0x
      description: 'Error budget for retrieval_latency_p95 is being consumed 6.0x
        faster than sustainable. Short window (30m) and long window (6h) both exceed
        threshold. Action: Page'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency_p95
  - alert: SLORetrieval_Latency_P95BurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(retrieval_service_search_duration_seconds_bucket{le=\"\
      0.25\"}[6h]))) / (sum(rate(retrieval_service_search_duration_seconds_count[6h])))))\
      \ > 0.050000000000000044\n            and\n            (1 - ((sum(rate(retrieval_service_search_duration_seconds_bucket{le=\"\
      0.25\"}[3d]))) / (sum(rate(retrieval_service_search_duration_seconds_count[3d])))))\
      \ > 0.050000000000000044\n        )"
    for: 2m
    labels:
      severity: warning
      slo: retrieval_latency_p95
      burn_rate: '1.0'
    annotations:
      summary: SLO retrieval_latency_p95 burn rate is 1.0x
      description: 'Error budget for retrieval_latency_p95 is being consumed 1.0x
        faster than sustainable. Short window (6h) and long window (3d) both exceed
        threshold. Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency_p95
  - alert: SLORetrieval_Latency_P95ErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - (sum(rate(retrieval_service_search_duration_seconds_bucket{le=\"\
      0.25\"}[30d]))) / (sum(rate(retrieval_service_search_duration_seconds_count[30d]))))\n\
      \            / 0.050000000000000044\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: retrieval_latency_p95
    annotations:
      summary: SLO retrieval_latency_p95 error budget exhausted
      description: 'The error budget for retrieval_latency_p95 has been completely
        consumed. Target: 95.0% over 30d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/retrieval_latency_p95
  - alert: SLORag_E2E_Latency_P95BurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_e2e_latency_seconds_bucket{le=\"2.0\"\
      }[5m]))) / (sum(rate(rag_e2e_latency_seconds_count[5m]))))) > 0.7200000000000006\n\
      \            and\n            (1 - ((sum(rate(rag_e2e_latency_seconds_bucket{le=\"\
      2.0\"}[1h]))) / (sum(rate(rag_e2e_latency_seconds_count[1h]))))) > 0.7200000000000006\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: rag_e2e_latency_p95
      burn_rate: '14.4'
    annotations:
      summary: SLO rag_e2e_latency_p95 burn rate is 14.4x
      description: 'Error budget for rag_e2e_latency_p95 is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/rag_e2e_latency_p95
  - alert: SLORag_E2E_Latency_P95BurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_e2e_latency_seconds_bucket{le=\"2.0\"\
      }[30m]))) / (sum(rate(rag_e2e_latency_seconds_count[30m]))))) > 0.30000000000000027\n\
      \            and\n            (1 - ((sum(rate(rag_e2e_latency_seconds_bucket{le=\"\
      2.0\"}[6h]))) / (sum(rate(rag_e2e_latency_seconds_count[6h]))))) > 0.30000000000000027\n\
      \        )"
    for: 2m
    labels:
      severity: critical
      slo: rag_e2e_latency_p95
      burn_rate: '6.0'
    annotations:
      summary: SLO rag_e2e_latency_p95 burn rate is 6.0x
      description: 'Error budget for rag_e2e_latency_p95 is being consumed 6.0x faster
        than sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/rag_e2e_latency_p95
  - alert: SLORag_E2E_Latency_P95BurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_e2e_latency_seconds_bucket{le=\"2.0\"\
      }[6h]))) / (sum(rate(rag_e2e_latency_seconds_count[6h]))))) > 0.050000000000000044\n\
      \            and\n            (1 - ((sum(rate(rag_e2e_latency_seconds_bucket{le=\"\
      2.0\"}[3d]))) / (sum(rate(rag_e2e_latency_seconds_count[3d]))))) > 0.050000000000000044\n\
      \        )"
    for: 2m
    labels:
      severity: warning
      slo: rag_e2e_latency_p95
      burn_rate: '1.0'
    annotations:
      summary: SLO rag_e2e_latency_p95 burn rate is 1.0x
      description: 'Error budget for rag_e2e_latency_p95 is being consumed 1.0x faster
        than sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/rag_e2e_latency_p95
  - alert: SLORag_E2E_Latency_P95ErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - (sum(rate(rag_e2e_latency_seconds_bucket{le=\"\
      2.0\"}[30d]))) / (sum(rate(rag_e2e_latency_seconds_count[30d]))))\n        \
      \    / 0.050000000000000044\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: rag_e2e_latency_p95
    annotations:
      summary: SLO rag_e2e_latency_p95 error budget exhausted
      description: 'The error budget for rag_e2e_latency_p95 has been completely consumed.
        Target: 95.0% over 30d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/rag_e2e_latency_p95
  - alert: SLOTenant_Error_RateBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_queries_total{status=\"success\"}[5m]))\
      \ by (tenant_id)) / (sum(rate(rag_queries_total[5m])) by (tenant_id)))) > 0.14400000000000013\n\
      \            and\n            (1 - ((sum(rate(rag_queries_total{status=\"success\"\
      }[1h])) by (tenant_id)) / (sum(rate(rag_queries_total[1h])) by (tenant_id))))\
      \ > 0.14400000000000013\n        )"
    for: 2m
    labels:
      severity: critical
      slo: tenant_error_rate
      burn_rate: '14.4'
    annotations:
      summary: SLO tenant_error_rate burn rate is 14.4x
      description: 'Error budget for tenant_error_rate is being consumed 14.4x faster
        than sustainable. Short window (5m) and long window (1h) both exceed threshold.
        Action: Page immediately'
      runbook_url: https://runbooks.example.com/slo/tenant_error_rate
  - alert: SLOTenant_Error_RateBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_queries_total{status=\"success\"}[30m]))\
      \ by (tenant_id)) / (sum(rate(rag_queries_total[30m])) by (tenant_id)))) > 0.06000000000000005\n\
      \            and\n            (1 - ((sum(rate(rag_queries_total{status=\"success\"\
      }[6h])) by (tenant_id)) / (sum(rate(rag_queries_total[6h])) by (tenant_id))))\
      \ > 0.06000000000000005\n        )"
    for: 2m
    labels:
      severity: critical
      slo: tenant_error_rate
      burn_rate: '6.0'
    annotations:
      summary: SLO tenant_error_rate burn rate is 6.0x
      description: 'Error budget for tenant_error_rate is being consumed 6.0x faster
        than sustainable. Short window (30m) and long window (6h) both exceed threshold.
        Action: Page'
      runbook_url: https://runbooks.example.com/slo/tenant_error_rate
  - alert: SLOTenant_Error_RateBurnRateTooHigh
    expr: "(\n            (1 - ((sum(rate(rag_queries_total{status=\"success\"}[6h]))\
      \ by (tenant_id)) / (sum(rate(rag_queries_total[6h])) by (tenant_id)))) > 0.010000000000000009\n\
      \            and\n            (1 - ((sum(rate(rag_queries_total{status=\"success\"\
      }[3d])) by (tenant_id)) / (sum(rate(rag_queries_total[3d])) by (tenant_id))))\
      \ > 0.010000000000000009\n        )"
    for: 2m
    labels:
      severity: warning
      slo: tenant_error_rate
      burn_rate: '1.0'
    annotations:
      summary: SLO tenant_error_rate burn rate is 1.0x
      description: 'Error budget for tenant_error_rate is being consumed 1.0x faster
        than sustainable. Short window (6h) and long window (3d) both exceed threshold.
        Action: Create ticket'
      runbook_url: https://runbooks.example.com/slo/tenant_error_rate
  - alert: SLOTenant_Error_RateErrorBudgetExhausted
    expr: "(\n        1 - (\n            (1 - (sum(rate(rag_queries_total{status=\"\
      success\"}[30d])) by (tenant_id)) / (sum(rate(rag_queries_total[30d])) by (tenant_id)))\n\
      \            / 0.010000000000000009\n        )\n    ) <= 0"
    for: 5m
    labels:
      severity: critical
      slo: tenant_error_rate
    annotations:
      summary: SLO tenant_error_rate error budget exhausted
      description: 'The error budget for tenant_error_rate has been completely consumed.
        Target: 99.0% over 30d. Immediate action required.'
      runbook_url: https://runbooks.example.com/slo/tenant_error_rate
